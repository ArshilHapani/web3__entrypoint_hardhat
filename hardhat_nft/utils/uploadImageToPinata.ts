export default async function uploadImageToPinata(): Promise<string[]> {
  try {
    const tokenUris: string[] = [];

    return tokenUris;
  } catch (error: any) {
    console.error(`Error in uploadImageToPinata: ${error}`);
    return [];
  }
}
